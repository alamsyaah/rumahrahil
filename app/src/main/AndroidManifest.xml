<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rumahrahil">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RumahRahil.Light">
        <activity android:name=".profile.EditProfileActivity" />
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".tes.TesKategoriActivity" />
        <activity android:name=".tes.TesActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".onboarding.OnboardingActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".onboarding.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>